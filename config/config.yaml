app:
  version: 0.0.0
  environment: development
  debug: true
  log_level: debug

server:
  host: 127.0.0.1
  port: 8080

database:
  host: localhost
  port: 5432
  user: splitkauf
  password: splitkauf
  name: splitkauf
  ssl_mode: disable
